# ワークフロー名を定義
name: Hello GitHub Actions

on:
  push:
    branches:
      -'main'

# ジョブを追加(最新バージョンのUbuntu上でビルドを実行)
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      -uses: actions/checkout@v3

      -name: Run a one-line script
       run: echo Hello Github Actions!!
